# Database — SQLite file path
DATABASE_URL="file:./prisma/dev.db"

# JWT — secret key for signing auth tokens (generate a random string)
JWT_SECRET="replace-with-a-random-secret-key"

# Admin — secret code required during registration to create admin accounts
ADMIN_SECRET_CODE="change-this-to-something-secure"
